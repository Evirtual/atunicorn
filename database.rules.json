{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && $uid === auth.uid && ((!data.child('approved').exists() && (!newData.child('approved').exists() || newData.child('approved').val() === false)) || newData.child('approved').val() === data.child('approved').val())",
        ".validate": "newData.hasChildren(['id', 'approved']) && newData.child('id').val() === $uid && newData.child('approved').isBoolean() && (!newData.child('username').exists() || newData.child('username').val() === data.child('username').val() || newData.parent().parent().child('usernames').child(newData.child('username').val()).val() === $uid) && (!data.child('username').exists() || data.child('username').val() === newData.child('username').val() || !newData.parent().parent().child('usernames').child(data.child('username').val()).exists())",
        "id": {
          ".validate": "newData.val() === $uid"
        },
        "approved": {
          ".validate": "newData.isBoolean()"
        },
        "username": {
          ".validate": "newData.isString() && newData.val() !== 'unicorn' && newData.val().matches(/^[a-z0-9]{3,15}$/)"
        }
      }
    },
    "usernames": {
      "$username": {
        ".write": "auth != null && root.child('metadata').child('usernamesReady').val() === true && $username !== 'unicorn' && $username.matches(/^[a-z0-9]{3,15}$/) && ((newData.val() === auth.uid && (!data.exists() || data.val() === auth.uid) && newData.parent().parent().child('users').child(auth.uid).child('username').val() === $username) || (!newData.exists() && data.val() === auth.uid && newData.parent().parent().child('users').child(auth.uid).child('username').val() !== $username))",
        ".validate": "newData.isString() && newData.val() === auth.uid"
      }
    },
    "posts": {
      ".read": true,
      "$uid": {
        "$postId": {
          ".write": "auth != null && $uid === auth.uid && (!newData.exists() || root.child('users').child(auth.uid).child('approved').val() === true)",
          ".validate": "newData.hasChildren(['id', 'userId', 'updated', 'username', 'nsfw']) && newData.child('id').isNumber() && newData.child('id').val() + '' === $postId && newData.child('userId').val() === auth.uid && newData.child('updated').isNumber() && newData.child('username').isString() && newData.child('nsfw').isBoolean()"
        }
      }
    }
  }
}
